//Grammer预留了接口，方便后续的扩展
/*
#include <iostream>
#include <vector>
#include"Para.h"
#include <string>
using namespace std;



bool Program(vector<Token>& TokenSq);
bool StmtList(vector<Token>& TokenSq);
bool Stmt(vector<Token>& TokenSq);
bool IfStmt(vector<Token>& TokenSq);
bool ElseIfList(vector<Token>& TokenSq);
bool ElseIf(vector<Token>& TokenSq);
bool ElsePart(vector<Token>& TokenSq);
bool WhileStmt(vector<Token>& TokenSq);
bool AssignStmt(vector<Token>& TokenSq);
bool Block(vector<Token>& TokenSq);
bool Expr(vector<Token>& TokenSq);
bool Expr1(vector<Token>& TokenSq);
bool Expr2(vector<Token>& TokenSq);
bool Expr3(vector<Token>& TokenSq);
bool Term(vector<Token>& TokenSq);
bool Factor(vector<Token>& TokenSq);


bool Program(vector<Token>& TokenSq) 
{
	if (StmtList(TokenSq)) 
	{
		return true;
	}
	else 
	{
		return false;
	}
}





void recursiveDecrise(vector<Token>& TokenSq, int index)
{

}
;

void Parse(vector<Token> &TokenSq,int index)
{
	//采用不同的方法解析TokenSq，index为采用的方法



}
*/